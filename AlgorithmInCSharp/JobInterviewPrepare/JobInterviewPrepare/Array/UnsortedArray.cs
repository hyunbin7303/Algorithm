using System;
using System.Collections.Generic;
using System.Text;

namespace JobInterviewPrepare.Array
{
    public class UnsortedArray
    {
        // Find majority element in an unsorted array.
        public static object GetMajorityElem(int [] arr)
        {

            return null;
        }

        // Another question
        // Find majority element (Boyer-moore majority Vote Algorithm)
        // Given an array of integers containing duplicates, return the majority element in an array if present.
        // A majority element appears more than n/2 times where n is the size of the array. 
        // e.g. {2,8,7,2,2,5,2,3,1,2,2}
        // My Solution.
        public static int GetMajorityElement(int [] arr)
        {
            int count = 0;
            for (int i = 0; i < arr.Length; i++)
            {
               
            }


            return 0;
        }
    }
}



//https://www.techiedelight.com/find-majority-element-in-an-array-boyer-moore-majority-vote-algorithm/