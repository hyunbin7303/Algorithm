
# Heaps can be used in sorting an array.
# in max-heaps, max element will always be at the root.

# max heap.
# It is similar to selection sort where we first find the max element
# and place the maximum element at the end.


# Binary Heap.
# takes the form of a binary tree.
# Binary heaps are a common way of implementing priority queues.
# 


def heapsort(arr):
    heapify(arr, len(arr), 0)
    
def heapify():
    """
    :param: arr - array to heapify
    n -- number of elements in the array
    i -- index of the current node
    TODO: Converts an array (in place) into a maxheap, a complete binary tree with the largest values at the top
    """
    






